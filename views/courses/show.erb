<<<<<<< HEAD
<form class="" action="/courses/<%= @course.course_id %>" method="post">
  <a class="btn btn-primary" href="/students/new">Add Student</a>
  <a class="btn btn-primary" href="">Edit Attendance</a>

  <input class="btn btn-primary" type="hidden" name="_method" value="delete">
  <input class="btn btn-primary" type="submit" value="Remove Course">
</form>
<hr>

<div class="">
  <% puts @todaysAttendance %>
  <ul>
    <% @course_attendance.each do |student_status|%>
    <form action="/courses/<%= @course.course_id %>" method="post">
      <div class="form-group">
        <input type="hidden" name="student_id" value="<%= student_status.student_id %>">
      </div>
      <div class="form-group">
        <label for="exampleFormControlInput1">Name</label>
        <h4 name="name"><a href="/students/<%= student_status.student_id %>"><%= student_status.first_name %> <%= student_status.last_name %></a></h4>
      </div>
      <div class="form-group">
        <label>Date</label>
        <input type="date" name="attendance_date" value="<%= Date.today %>">
      </div>
      <div class="form-group">
        <label>Status</label>
        <select class="form-control" name="status">
          <!-- Below is the autofill that doesnt work, use  "selected = "selected" -->
          <option>On Time</option>
          <option>Slightly late(5 minutes)</option>
          <option>Very late(over 30 minutes)</option>
          <option>Absent</option>
          <option selected="selected">Authorized Absence</option>
        </select>
      </div>
      <div class="form-group">
        <label>Description</label>
        <textarea class="form-control" name="description">
        </textarea>
      </div>
      <div class="form-group">
        <input type="submit" value="Save Attendance">
      </div>
    </form>
=======
<div class="main-content">
  <h2 class="group-title"><%= @course[0].name %> Register</h2>

  <div class="buttons">
    <button type="button" name="button"><a href="/students/new">Add Student</a></button>
    <button type="button" name="button">Delete Course</button>
  </div>

  <div class="form-heading">
    <p id="name-col">Name</p>
    <p id="date-col">Date</p>
    <p id="att-col">Attendance</p>
    <p id="description-col">Description</p>
  </div>

  <% @students.each do |student|%>
  <form method="post" class="student-attendance-display">

    <% if(student.student_id) %>
      <input type="hidden" name="_method" value="put">
>>>>>>> course_form_styling
    <% end %>

    <div class="form-group student-form">
      <input type="hidden" name="student_id" value="<%= student.student_id %>">
      <p class="student-name" name="date"><%= student.first_name %> <%= student.last_name %></p>
      <p><%= Date.today.to_s %></p>
      <select class="form-control" name="status" id="exampleFormControlSelect2">
        <option>On Time</option>
        <option>Slightly late(5 minutes)</option>
        <option>Very late(over 30 minutes)</option>
        <option>Absent</option>
        <option>Authorized Absence</option>
      </select>
      <textarea class="form-control" name="description" id="exampleFormControlTextarea1"></textarea>
      <input type="submit" value="Save Attendance">
    </div>
  </form>
  <% end %>
</div>
