<div class="main-content">
  <h2 class="group-title"><%= @course[0].name %> Register</h2>

  <div class="buttons">
    <button type="button" name="button"><a href="/students/new">Add Student</a></button>
    <button type="button" name="button">Delete Course</button>
  </div>

  <div class="form-heading">
    <p id="name-col">Name</p>
    <p id="date-col">Date</p>
    <p id="att-col">Attendance</p>
    <p id="description-col">Description</p>
  </div>

  <% @students.each do |student|%>
  <form method="post" class="student-attendance-display">

    <% if(student.student_id) %>
      <input type="hidden" name="_method" value="put">
    <% end %>

    <div class="form-group student-form">
      <input type="hidden" name="student_id" value="<%= student.student_id %>">
      <p class="student-name" name="date"><%= student.first_name %> <%= student.last_name %></p>
      <p><%= Date.today.to_s %></p>
      <select class="form-control" name="status" id="exampleFormControlSelect2">
        <option>On Time</option>
        <option>Slightly late(5 minutes)</option>
        <option>Very late(over 30 minutes)</option>
        <option>Absent</option>
        <option>Authorized Absence</option>
      </select>
      <textarea class="form-control" name="description" id="exampleFormControlTextarea1"></textarea>
      <input type="submit" value="Save Attendance">
    </div>
  </form>
  <% end %>
</div>
